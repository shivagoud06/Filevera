import { pageMetadata } from "@/lib/seo";
import type { ReactNode } from "react";
export const metadata = pageMetadata("/merge-pdf", "Merge PDF Files Online", "Combine multiple PDF files into one document in the order you choose.");
export default function MergePdfLayout({ children }: { children: ReactNode }) { return children; }